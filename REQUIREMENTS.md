# Dev Tools 功能需求

## 0. 整体布局

- 应用采用侧边栏 + 主内容区 + 底部状态栏的三栏布局。
- 左侧为工具菜单侧边栏（Sidebar），列出所有工具入口：TSV 转 SQL、图片转 Base64、JSON 格式化、JSON5 格式化、JSON 比较等。
- 设置页面不再放在侧边栏中，也不在主内容区中打开，而是通过系统菜单栏（macOS 顶部菜单栏）的"Dev Tools → 设置…"入口或快捷键 `Cmd+,` 打开一个独立的弹窗。
- 设置弹窗使用 gpui-kit 的 `Dialog` 或 `Sheet` 组件，以模态弹窗形式展示，不干扰主内容区的工具操作。
- 弹窗关闭后焦点回到之前的工具页面。
- 中间为主内容区，根据侧边栏选中的工具或打开设置页面展示对应的功能页面。
- 底部为状态栏（StatusBar），展示应用状态信息（如版本号、当前工具名称等）。
- 侧边栏和状态栏尺寸自适应，主内容区填满剩余空间。
- 所有工具页面在加载数据后，内容区域应支持垂直滚动，确保用户可访问超出视口的全部内容。
- 当工具模块包含超过 2 个源文件时，应创建独立目录（`src/tools/<工具名>/`）而非单文件，保持代码组织清晰。

## 1. TSV 转 SQL IN 条件

### 功能说明

- 提供 TSV 文本输入框，支持直接粘贴 TSV 数据。
- 提供可选的分隔符输入框，用户可自定义分隔符（如逗号、空格、竖线等）。填写分隔符后，数据按该分隔符切分；未填写时默认按 Tab（`\t`）切分。
- 用户选择需要转换的列号（从 1 开始），工具从切分后的数据中提取对应列。
- 用户点击“转换”按钮后，将 TSV 数据转换为可用于 SQL `IN` 条件的内容。
- 将固定前缀和外层括号拆分为两个独立选项，用户可以分别控制。
- 提供“固定前缀”输入框：默认内容为 `IN `，用户可以启用、禁用或修改为其他固定文本。
- 提供“添加外层括号”选项，单独控制结果是否使用 `()` 包裹。
- 两个选项默认均关闭；默认只输出可放入 SQL `IN (...)` 中的值列表。
- 转换结果应便于直接复制到 SQL 语句中使用。

### 示例

输入：

```tsv
1001	Alice
1002	Bob
```

当用户选择第一列进行转换，且固定前缀和外层括号均关闭时，输出：

```sql
'1001', '1002'
```

启用固定前缀 `IN `，但不添加外层括号时，输出：

```sql
IN '1001', '1002'
```

不启用固定前缀，但添加外层括号时，输出：

```sql
('1001', '1002')
```

同时启用固定前缀 `IN `和外层括号时，输出：

```sql
IN ('1001', '1002')
```

用户也可以把固定前缀修改为其他内容。例如输入 `NOT IN `并启用外层括号，可输出：

```sql
NOT IN ('1001', '1002')
```

两个选项均关闭时，用户可以自行把值列表放入 SQL 条件中，例如 `WHERE id IN ('1001', '1002')`。

### 转换规则

- 提供可选的自定义分隔符：未填写时默认按 Tab（`\t`）切分；填写后按填写的字符/字符串切分。
- 用户选择需要转换的列号（从 1 开始），工具从切分后的数据中提取对应列。
- 除 `NULL` 外，数字和字符串等数据统一按字符串处理，并添加单引号。
- `NULL` 作为 SQL 空值处理，不添加单引号。
- 自动去除重复值。
- 自动过滤空值。
- 自动转义字符串中的单引号，确保生成的内容可以安全地用于 SQL `IN` 条件。

## 2. 图片转 Base64

### 功能说明

- 在“图片”菜单下新增“图片转 Base64”工具。
- 支持选择或拖入图片，并将图片转换为 Base64 编码。
- 展示转换结果，并提供一键复制功能。
- 图片预览应根据预览区域自动等比例缩放，完整显示图片且不拉伸、不裁剪。
- 窗口或预览区域尺寸变化时，图片尺寸应随之自动调整。
- 图片尺寸小于预览区域时不强制放大，避免低分辨率图片变得模糊。
- 建议同时支持输出纯 Base64 内容和 Data URL，例如：

```text
data:image/png;base64,...
```

## 3. JSON 与 JSON5 格式化

### 功能说明

- 新增 JSON 格式化工具。
- 新增 JSON5 格式化工具，支持 JSON5 语法的解析与格式化。
- 提供输入、格式化结果展示及一键复制功能。
- 输入区域使用 `Editor` 组件，支持多行代码编辑、语法高亮，可容纳大量 JSON 内容。
- 当输入内容无法解析时，使用 Alert 组件展示清晰的错误信息，并指出错误开始的字符位置（行号 + 列号）。

## 4. JSON 增强功能

### 4.1 Key 排序

- 支持按 Key 对 JSON 对象进行排序。
- 支持递归排序，即嵌套对象中的 Key 也可以排序。
- 数组中的对象也应支持 Key 排序，但不改变数组元素原有顺序。
- 排序后应保持数据值和层级结构不变。

### 4.2 自动转义

- JSON 格式化时支持自动转义，或提供独立的“自动转义”按钮供用户手动触发。
- 支持处理字符串中的双引号、反斜杠、换行符、制表符等需要转义的字符。
- 自动转义后应生成合法、可解析的 JSON 内容。
- 已正确转义的内容不应被重复转义。
- 建议同时提供“取消转义”功能，方便在转义文本和原始文本之间转换。

### 4.3 JSON 压缩

- 支持将格式化后的 JSON 压缩为单行内容。
- 移除不影响数据含义的缩进、换行和多余空白字符。
- 压缩前后 JSON 的数据、类型及层级结构必须保持一致。
- 提供独立的”压缩”按钮，并支持一键复制压缩结果。
- 输入不是合法 JSON 时，应显示明确的错误信息和错误位置。

## 5. JSON 比较

### 功能说明

- JSON 比较独立于 JSON 格式化工具，作为一个单独的工具菜单项。
- 页面采用左右分屏布局：左侧输入基准 JSON（旧值），右侧输入新 JSON（新值），两个输入框水平并排。
- 左右输入区域均使用 `Editor` 组件，支持多行代码编辑、语法高亮，可容纳大量 JSON 内容。
- 比较前自动对两侧 JSON 做 Key 递归排序，确保 diff 结果不受 Key 顺序影响。
- 比较结果直接在左右两侧输入框中展示，不使用单独的 diff 结果页面。
- 左右两侧按排序后的 Key 对齐展示：若某 Key 在一侧存在而另一侧缺失，缺失侧对应位置留空一行。
- 每个输入框左侧显示行号，行号与内容一一对齐。
- 差异项通过颜色/标记直观区分新增、删除和修改的内容。
- 保留比较前的原始 JSON（格式化后），默认收起，用户可点击展开查看。
- 提供一键复制 diff 结果的功能。
- 当任意一侧 JSON 格式不正确时，使用 Alert 组件展示清晰的错误信息，并指出错误开始的字符位置（行号 + 列号）。

## 6. 系统菜单栏

- 应用注册 macOS 系统菜单栏（顶部菜单栏），菜单名为"Dev Tools"。
- 菜单项包括：
  - "设置…"：打开设置页面，右侧显示快捷键 `Cmd+,`。
  - 分隔线。
  - "退出 Dev Tools"：退出应用，右侧显示快捷键 `Cmd+Q`。
- 快捷键注册在应用全局，无论当前焦点在何处均可使用。

## 7. 界面设置

使用 gpui-kit 的 `Settings` 组件构建设置面板，作为独立弹窗（Dialog/Sheet），通过系统菜单栏或快捷键 `Cmd+,` 打开，不在侧边栏或主内容区中显示。结构如下：

```
Settings (侧边栏导航 + 内容区)
  SettingPage         ← 每个设置分类为一个页面，通过侧边栏切换
    SettingGroup
      SettingItem
        Label / Description
        SettingField (Switch / Radio / Dropdown / Input)
```

### 7.1 Settings 整体结构

- 设置面板通过 `Settings::new(id)` 创建，包含多个 `SettingPage`。
- 左侧为可搜索的侧边栏导航，列出所有设置页面；右侧为当前选中页面的内容区。
- 侧边栏支持搜索过滤，输入关键词后自动筛选匹配的设置项。
- 每个 `SettingPage` 可设置图标、标题、描述，并包含多个 `SettingGroup`。
- 每个 `SettingGroup` 可设置标题、描述，并包含多个 `SettingItem`。
- 每个 `SettingItem` 由标签/描述和对应的设置字段（`SettingField`）组成。
- 页面支持重置按钮，一键恢复该页所有设置为默认值。

### 7.2 菜单位置

- 作为 `SettingPage` 下的一个 `SettingGroup`，标题为”菜单位置”。
- 使用 `SettingItem::new` + `Switch` 字段组件，切换菜单在”左侧”和”右侧”之间显示。
- 开关上显示当前选中的位置。
- 切换后界面应立即更新，不需要重新启动应用。
- 保存用户选择，下次启动应用时继续使用上一次的菜单位置。

### 7.3 主题模式

- 作为 `SettingPage` 下的一个 `SettingGroup`，标题为”主题模式”。
- 使用 `SettingItem::new` + `Select` 字段组件，提供三种主题选项：白色、黑色和跟随系统。
- 同一时间只能选中一个主题。
- “白色”对应浅色主题，”黑色”对应深色主题。
- “跟随系统”应根据操作系统当前的外观设置自动选择浅色或深色主题。
- 系统外观发生变化时，”跟随系统”模式下的应用主题应同步更新。
- 保存用户选择，下次启动应用时继续使用上一次的主题模式。

### 7.4 字体

- 作为 `SettingPage` 下的一个 `SettingGroup`，标题为”字体”。
- 代码/等宽文本输出区域（如 Base64 输出、SQL 值列表、JSON 格式化结果）使用 JetBrains Mono 等宽字体。
- 用户系统应安装 JetBrains Mono 字体，以确保界面显示效果与设计一致。

## 验收要点

- 应用采用侧边栏 + 主内容区 + 状态栏的三栏布局。
- 左侧侧边栏列出所有工具菜单（TSV 转 SQL、图片转 Base64、JSON 格式化、JSON 比较），点击可切换主内容区功能。
- 底部状态栏正常显示应用状态信息。
- 所有工具均可从侧边栏菜单进入并正常使用。
- 转换或格式化结果准确，且支持一键复制。
- TSV 转 SQL 支持独立启用或禁用固定前缀和外层括号，并允许修改固定前缀内容；不同组合的输出均符合对应格式。
- 图片预览能随预览区域等比例缩放，并完整显示图片、不拉伸、不裁剪。
- 无效输入不会导致程序崩溃，并能给出明确提示。
- JSON 格式化工具输入非法 JSON 时，错误信息包含行号、列号及字符位置。
- JSON 格式化工具输入区域使用 `Editor` 组件，支持语法高亮和多行编辑。
- JSON Key 排序可覆盖所有嵌套对象。
- JSON 自动转义结果合法，且不会对已转义字符进行重复转义。
- JSON 压缩结果为单行合法 JSON，且不改变原始数据含义。
- JSON 比较工具独立于 JSON 格式化工具，可从菜单进入并正常使用。
- JSON 比较前自动排序 Key，diff 结果不受 Key 顺序影响。
- JSON 比较结果直接在左右两侧输入框中展示，无单独 diff 结果页面。
- 比较结果按 Key 对齐，缺失侧留空；每个输入框左侧显示行号。
- 差异项通过颜色/标记直观区分新增、删除和修改项。
- JSON 比较页面保留原始 JSON（格式化后），默认收起，点击可展开查看。
- JSON 比较工具输入非法 JSON 时，使用 Alert 组件展示错误信息，包含行号、列号及字符位置，且两侧分别检测报错。
- JSON 比较工具左右输入区域均使用 `Editor` 组件，支持语法高亮和多行编辑。
- 系统菜单栏显示"Dev Tools"菜单，包含"设置…"和"退出 Dev Tools"项。
- 系统菜单栏"设置…"项右侧显示快捷键 `Cmd+,`。
- 按下 `Cmd+,` 快捷键可打开设置弹窗。
- 按下 `Cmd+Q` 快捷键可退出应用。
- 设置面板通过系统菜单栏或快捷键 `Cmd+,` 打开，以独立弹窗形式展示，不在侧边栏或主内容区中显示。
- 设置弹窗关闭后焦点回到之前的工具页面。
- 设置面板使用 gpui-kit Settings 组件构建，包含侧边栏导航和内容区。
- 侧边栏可搜索过滤，输入关键词后自动筛选匹配的设置项。
- 设置页面支持重置按钮，一键恢复该页所有设置为默认值。
- 菜单可在左侧和右侧之间切换，重启应用后仍保留用户选择。
- 三种主题均可正常切换；跟随系统模式能正确响应系统外观变化。
